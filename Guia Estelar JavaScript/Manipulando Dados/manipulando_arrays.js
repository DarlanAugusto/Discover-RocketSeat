// Manipulando Arrays


let word = "paralelepípedo";

console.log("Palavra:", word);
console.log("Array da palavra:", Array.from(word));
console.log("Palavra separada por traços:", Array.from(word).join("-"));
console.log("Palavra separada por traços, em caixa alta:", Array.from(word).join("-").toUpperCase());